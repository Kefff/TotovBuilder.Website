import { describe, it } from 'vitest'

describe('CompatibilityUtils', () => {
  describe('checkCompatibility', () => {
    it('should do nothing when the item to add is not a mod nor in an inventory slot', () => {
      // Arrange

      // Act

      // Assert
    })

    it('should check compatibility between items in inventory slots', () => {

    })
  })
})